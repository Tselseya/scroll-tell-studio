# Self-hosted open-source image and video generation for n8n

**Research snapshot: 2026-09-26.** This guide replaces the outdated/unsupported project list in the supplied draft with a short, source-checked list of projects that have runnable local inference paths and a practical n8n integration route.

> **Important:** “Free/open source” does not mean zero-cost or unrestricted. The software can be free to download while compute, power, storage, and network hosting still cost money. **Code and model weights are separately licensed.** Check the exact checkpoint, precision variant, text encoder, VAE, LoRA, custom node, and version before use, especially for commercial deployment. This review confirmed documented run paths; it did not independently run each large model.

## Quick recommendation

- **Simplest common HTTP automation surface:** run [ComfyUI](https://github.com/Comfy-Org/ComfyUI) locally or on a private GPU host. ComfyUI has documented HTTP and WebSocket server routes, including a workflow submission endpoint. Install a model/workflow compatible with the task; ComfyUI itself is a workflow runner, **not one model**.
- **Text-to-image:** [InvokeAI](https://github.com/invoke-ai/InvokeAI) is a practical self-hosted image-generation application with a documented local workflow API. Alternatively, use ComfyUI with a specific image model such as [Qwen-Image](https://github.com/QwenLM/Qwen-Image) and its ComfyUI workflow.
- **Text-to-video / image-to-video:** [Wan2.1](https://github.com/Wan-Video/Wan2.1) has official local inference code and official ComfyUI workflows for both tasks. ComfyUI's documented Wan workflows make it a convenient way to expose multiple Wan tasks through the same automation API. Expect substantial GPU and disk requirements; the modest 8.19 GB figure applies only to a particular 1.3B, 480p text-to-video setup—not a universal minimum or an estimate for Wan 14B.
- **Other viable video implementations:** [CogVideoX](https://github.com/zai-org/CogVideo) supports local T2V and I2V but has separate model-weight terms and looked comparatively quiet in this review. [HunyuanVideo](https://github.com/Tencent-Hunyuan/HunyuanVideo) is runnable for T2V but the official cited settings peak at 45–60 GB GPU memory and its community license has significant territorial and use restrictions. [LTX-Video](https://github.com/Lightricks/LTX-Video) is a runnable older T2V/I2V line; its README points to LTX-2 for newer development. Check exact LTX weight terms and access gates.

## Curated projects by task

| Task | Project/repository | What it actually does | n8n route | License / practical caveat |
|---|---|---|---|---|
| Text-to-image | [InvokeAI](https://github.com/invoke-ai/InvokeAI) | Self-hosted image-generation application supporting multiple local model families; also documents experimental video workflows. | InvokeAI documents a local HTTP workflow API: submit an executable graph to `/api/v1/queue/default/enqueue_batch`, poll queue state, then retrieve output. Call it with n8n HTTP Request nodes. This is not a first-party n8n node. | Repository code is Apache-2.0; each checkpoint has its own terms. Official examples include SDXL (Open RAIL++-M) and gated FLUX.1 [dev] (non-commercial license). Some listed provider models are hosted/API-only rather than local weights. Requirements vary by model: InvokeAI lists examples from 4 GB VRAM for SD 1.5, 8 GB for SDXL, and 10 GB for FLUX.1; these are example configurations, not guarantees. [API](https://invoke.ai/development/guides/workflow-api/) · [requirements](https://invoke.ai/start-here/system-requirements/) · [model terms](https://invoke.ai/concepts/models/) |
| Text-to-image | [ComfyUI](https://github.com/Comfy-Org/ComfyUI) + [Qwen-Image](https://github.com/QwenLM/Qwen-Image) | ComfyUI is an actively maintained, open-source workflow engine; Qwen-Image is a model with an official ComfyUI T2I workflow. | ComfyUI's local API: `POST /prompt` with an executable API-format workflow; use `/history/{prompt_id}` or queue status to check completion; retrieve saved image output through `/view`. WebSocket `/ws` provides live progress. | ComfyUI code is GPL-3.0. That does not determine model/asset rights. ComfyUI's Qwen docs describe Qwen-Image weights as Apache-2.0 and give a tested fp8 workflow memory example; it is a large model and not a universal low-VRAM option. [ComfyUI API](https://docs.comfy.org/development/comfyui-server/api-examples) · [Qwen workflow](https://docs.comfy.org/tutorials/image/qwen/qwen-image) |
| Text-to-video | [Wan2.1](https://github.com/Wan-Video/Wan2.1) | Official repository includes runnable T2V code/checkpoints; Wan2.1 also has I2V models. | The Wan repository documents Python/CLI and Gradio paths, not a model-native REST API. Use an official [ComfyUI Wan workflow](https://comfyanonymous.github.io/ComfyUI_examples/wan/) and call ComfyUI's API, or wrap the Python inference in a private job API. | Code license is Apache-2.0; model card identifies Wan2.1 weights as Apache-2.0, with user compliance duties also stated. Official README's 8.19 GB statement is specifically Wan2.1 T2V-1.3B at 480p; its 1.3B workflow is less stable at 720p. Larger models/settings need more resources. [repo README](https://github.com/Wan-Video/Wan2.1) · [T2V card](https://huggingface.co/Wan-AI/Wan2.1-T2V-14B) · [I2V card](https://huggingface.co/Wan-AI/Wan2.1-I2V-14B-720P) |
| Image-to-video | [Wan2.1 I2V](https://github.com/Wan-Video/Wan2.1) | Conditions generated video on an input still; official 480p and 720p I2V checkpoints/workflows exist. | Use a ComfyUI Wan I2V workflow and pass the input still through the workflow's image-input node, then submit/poll/retrieve via the same ComfyUI API. A direct local Python inference wrapper is another option. | Same code/weights distinction and resource caveats as above. Model, resolution, frame count, precision, and offloading change requirements materially. |
| Image-to-video | [Stable Video Diffusion (SVD/SVD-XT)](https://github.com/Stability-AI/generative-models) | Research-oriented, runnable image-to-video checkpoints for short clips (SVD 14 frames; XT 25 frames in documented configurations). | First-party docs show Python/Diffusers and demo paths, not a production REST contract or n8n node. Use a wrapper or a compatible ComfyUI workflow; validate any third-party node separately. | Code is MIT; the checkpoint is separately governed by Stability AI terms. SVD-XT's community license includes noncommercial/commercial and revenue conditions. The official card says **text does not control the model**; do not call this T2V. Clips are short and may have little/no motion or artifacts. [model card](https://huggingface.co/stabilityai/stable-video-diffusion-img2vid-xt) · [checkpoint license](https://huggingface.co/stabilityai/stable-video-diffusion-img2vid-xt/blob/main/LICENSE.md) |
| T2V + I2V | [CogVideoX](https://github.com/zai-org/CogVideo) | Local Python/Diffusers inference for T2V and I2V variants; runnable code is documented. | The official sources document CLI/Diffusers, not an official n8n REST integration. Wrap inference in an API/job service or use a separately maintained ComfyUI integration after checking its compatibility. | Code is Apache-2.0; weights use a separate CogVideoX License. The reviewed terms require commercial registration/licensing and include other restrictions. Official docs' VRAM estimates depend on model/precision/optimizations. Repository is not formally archived but appeared comparatively quiet in this snapshot. [weights license](https://github.com/zai-org/CogVideo/blob/main/MODEL_LICENSE) |
| Text-to-video | [HunyuanVideo](https://github.com/Tencent-Hunyuan/HunyuanVideo) | Runnable local T2V code and demo. This is not the separate HunyuanVideo-I2V repository. | First-party code documents CLI/Gradio, not a first-party n8n REST API. Use a private inference wrapper or compatible ComfyUI workflow. | Governed by the Tencent Hunyuan Community License, not MIT/Apache or unrestricted. It excludes EU, UK, and South Korea from its stated territory and has other use restrictions. The official cited settings use 45–60 GB peak GPU memory. [license](https://raw.githubusercontent.com/Tencent-Hunyuan/HunyuanVideo/main/LICENSE.txt) · [hardware/setup](https://github.com/Tencent-Hunyuan/HunyuanVideo) |
| Older T2V + I2V | [LTX-Video](https://github.com/Lightricks/LTX-Video) | Runnable Python inference for text-to-video and image-to-video; **video-only**, not a T2I model. The README points users to LTX-2 as the newer development home. | The repo documents Python/CLI and ComfyUI workflows; no first-party n8n node or model-native REST service was found. For automation, call ComfyUI's API or create a wrapper. | Code is Apache-2.0, but checkpoint licenses vary by version. Later LTX-Video weights have separate open-weights terms, including restrictions and a stated paid-commercial threshold. Newer [LTX-2.5](https://huggingface.co/Lightricks/LTX-2.5) access is gated and governed by Lightricks' LTX-2.x Community License. [legacy repo](https://github.com/Lightricks/LTX-Video) · [weights terms](https://huggingface.co/Lightricks/LTX-Video) |

**How to read the table:** “Runnable” means the official project documents local code/UI/model usage; it does not mean generation was executed as part of this review, nor that every listed model can run on a typical laptop. For video generation, frame count, resolution, temporal length, precision and offloading all affect memory and throughput. Check the model-specific install instructions and current model card before downloading.

## Connect ComfyUI to n8n

ComfyUI's documented local server defaults to `http://127.0.0.1:8188`. It accepts executable workflow JSON at `POST /prompt`, returns a `prompt_id`, supports queue/history routes, and exposes `GET /view` for saved output. Its WebSocket `/ws` reports progress. ComfyUI's official docs do **not** list a native n8n node; the standard n8n HTTP Request node is sufficient for REST calls.

A practical n8n workflow is:

1. **Trigger** (Webhook, form, schedule, or manual) provides the prompt and any input image.
2. **Prepare workflow JSON** (Edit Fields/Code node or a stored template) using ComfyUI's **API format** export. Set the model-specific positive prompt, seed, resolution/duration/frame count, and input asset fields. Keep custom node versions and models aligned with the workflow.
3. **HTTP Request — queue**: `POST http://COMFYUI_HOST:8188/prompt`, body type JSON, with `{ "prompt": <API-format workflow> }` (optionally include a unique `client_id`). Save the returned `prompt_id`.
4. **Wait + HTTP Request — status**: poll `GET http://COMFYUI_HOST:8188/history/<prompt_id>` (or queue state) until the workflow is complete. Use a bounded retry/count/timeout and an error branch rather than an infinite loop. A WebSocket client can provide progress, but REST polling is easier with stock n8n nodes.
5. **HTTP Request — fetch output**: use the file details returned in history to request `GET http://COMFYUI_HOST:8188/view?filename=...&subfolder=...&type=...`. In n8n, set the response format to **File** to place the image/video into binary data; send/store it in a following node or upload it to M.O.S.A.N.G.
6. Handle failed generation, timeout, unsupported nodes, and missing output explicitly.

Official references: [ComfyUI server API examples](https://docs.comfy.org/development/comfyui-server/api-examples), [ComfyUI server routes](https://docs.comfy.org/development/comfyui-server/comms_routes), and [n8n HTTP Request node](https://docs.n8n.io/integrations/builtin/core-nodes/n8n-nodes-base.httprequest).

For InvokeAI, use its documented local HTTP workflow API, then poll job/queue state and retrieve the generated asset. For model projects that expose only Python/CLI or Gradio, **do not point HTTP Request at a notebook/UI and assume it is a stable inference API**. Put a small authenticated job service in front: accept prompt/input, return a job ID, run the GPU inference off the request thread, then expose status and an authenticated file-download endpoint. A callback to an n8n Webhook is optional.

### Network and security notes

- If n8n is cloud-hosted, its `127.0.0.1` is **not** the user's computer. Put n8n and ComfyUI on the same private network, or expose a secured HTTPS service/VPN endpoint reachable from the n8n host.
- Avoid exposing ComfyUI's unauthenticated local server directly to the public internet. Use a firewall/private network or authenticated reverse proxy and restrict who can upload inputs or queue arbitrary jobs.
- Keep prompts, generated files, logs, and API credentials private; volunteer generation services and third-party workers may see submitted inputs.
- A long video generation should be asynchronous: queue, persist the job ID, poll at bounded intervals, retrieve the file, and apply explicit retry/idempotency handling. Do not leave one HTTP request hanging while a large model generates.

## Hosted free option is not unlimited

[AI Horde](https://aihorde.net/) is a community-run image-generation API served by volunteer workers. Its official docs say generation is free (including anonymously) **subject to request limits and available workers**. Model availability, queue time, request requirements, and worker compatibility vary; a request can expire if no worker becomes available. It is neither a locally self-hosted generator nor an unlimited/guaranteed API. Treat prompts and images as potentially visible to the volunteer worker processing the job. See [How AI Horde works](https://www.stablehorde.net/docs/image/learn/concepts_horde) and [request limits/model availability](https://www.stablehorde.net/docs/image/learn/concepts_migrating/).

## Fit for this machine: i5-1035G7, 8 GB RAM, Docker Desktop

This assessment uses the supplied detector output and Docker Desktop screenshot:

- **CPU:** Intel Core i5-1035G7, 4 physical cores / 8 threads, no usable GPU reported by the detector.
- **Host RAM:** 8 GB total.
- **Docker Desktop memory:** screenshot shows about **3.54 GB total** available to containers and **1.95 GB in use** at capture time—only about **1.6 GB free**, shared by the displayed services (including Open WebUI, Whisper, TTS, n8n, and database/support containers). Container use will fluctuate.
- **“Max model size: 3 GB”** is a heuristic from the detector, not a guarantee that any 3 GB checkpoint will fit in RAM. Download size, runtime working memory, and inference speed are different quantities.

### Hardware detection discrepancy: this CPU normally includes an iGPU

Intel's official specification lists **Intel Iris Plus Graphics** for the i5-1035G7, with a 300 MHz base and up to 1.05 GHz graphics frequency ([Intel specification](https://www.intel.com/content/www/us/en/products/sku/196592/intel-core-i51035g7-processor-6m-cache-up-to-3-70-ghz/specifications.html)). So “Integrated GPUs: None” should be read as **no integrated GPU detected/available to the software path that produced this report**, not proof that the processor silicon has no graphics unit. Verify Windows Device Manager, OEM/BIOS settings, and drivers. Docker Desktop's Linux environment will not necessarily expose the Windows iGPU to a container automatically. Until a supported driver/pass-through path is confirmed, plan for CPU-only inference; Iris Plus is also not an NVIDIA CUDA GPU, and its presence does not imply fast diffusion generation.

### Practical fit by option

| Option | Fit on the currently shown setup | Why / operating conditions |
|---|---|---|
| [stable-diffusion.cpp](https://github.com/leejet/stable-diffusion.cpp) + quantized SD 1.5 | **Best local experiment; currently memory-borderline** | Explicit CPU x86 AVX/AVX2 builds and a native asynchronous HTTP server. Upstream reports about 2.0 GB runtime memory for one SD 1.x Q4/Q5 512×512 setup, or about 1.5 GB with Flash Attention. The screenshot shows only about 1.6 GB unused across Docker before loading the model, so do not expect this to run reliably alongside the current containers without more Docker memory or stopping workloads. A Q4_0 SD 1.5 GGUF file is listed around 3.05 GB **on disk**—not its RAM requirement. Treat throughput as slow/unknown; there is no benchmark for this CPU. Sources: [build/CPU support](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/build.md), [GGUF memory guidance](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/quantization_and_gguf.md), [SD 1.5 GGUF card](https://huggingface.co/kostakoff/stable-diffusion-v1-5-GGUF). |
| [OpenVINO GenAI SD 1.5 INT8](https://huggingface.co/OpenVINO/stable-diffusion-v1-5-int8-ov) via [OpenVINO Model Server (OVMS)](https://docs.openvino.ai/2026/model-server/ovms_demos_image_generation.html) | **Possible second experiment; memory and speed unverified** | CPU generation is documented and OVMS provides HTTP image endpoints for n8n. The model artifact is listed around 2.3 GB on disk, but that is not peak RAM; compilation can itself be memory-intensive. Official docs provide no guarantee for a 3.5 GB cap or an i5-1035G7. OVMS adds its own runtime overhead. |
| [Segmind Tiny-SD](https://huggingface.co/segmind/tiny-sd) with the [OpenVINO CPU example](https://docs.openvino.ai/2023.3/notebooks/251-tiny-sd-image-generation-with-output.html) | **Smallest model download; experimental** | The principal files total about 1.06 GB on disk and a CPU/OpenVINO path is documented. CPU execution uses float32 in the cited guidance; estimated working memory is larger than file size, with no official peak-RAM or speed figure. It may be worth an isolated 512×512 test, but the 3.5 GB Docker ceiling and other active containers make fit uncertain. Segmind characterizes it as an early-stage distilled model with quality/generalization limitations. |
| [InvokeAI](https://github.com/invoke-ai/InvokeAI) | **Not recommended under the current cap** | InvokeAI does have CPU paths, but its rough SD 1.5 guidance lists 8 GB RAM for 512×512, while the Docker budget in the screenshot is about 3.54 GB total across containers. Its docs describe local generation without a GPU as slow. A future test would need considerably more Docker/host headroom; this is not a claim that CPU inference is categorically impossible. [Requirements](https://invoke.ai/start-here/system-requirements/) · [Docker configuration](https://invoke.ai/configuration/docker/). |
| [CogVideoX](https://github.com/zai-org/CogVideo) or other true local T2V/I2V models | **Not a practical local option** | CogVideoX's cited 2B Diffusers repository is around 13.8 GB total on disk; the GPU-memory figures published for optimized inference are VRAM figures, not CPU RAM requirements. First-party material supplies no reliable i5 CPU timing or 3.5 GB system-memory fit, and an upstream issue describes Intel as unsupported in the discussed CPU/MPS context. Its dedicated I2V checkpoints are larger 5B variants. Do not install it in this Docker stack expecting CPU-only video generation. [Model](https://huggingface.co/zai-org/CogVideoX-2b) · [upstream issue](https://github.com/zai-org/CogVideo/issues/222). |

### Recommended workflow on the current machine

1. **For local image experiments:** consider a single queued `stable-diffusion.cpp` CPU job with a compatible quantized SD 1.5 checkpoint, 512×512 output, one image at a time, and conservative sampling settings. First stop idle memory-heavy containers or increase Docker Desktop's memory allocation; the screenshot's current ~1.6 GB free is less than the upstream example runtime figure even before overhead. Measure peak memory and elapsed time on this exact setup; no published benchmark predicts the i5-1035G7's image time.
2. **For n8n:** `stable-diffusion.cpp` documents an asynchronous API at `POST /sdcpp/v1/img_gen`, followed by `GET /sdcpp/v1/jobs/{id}` polling; completed image bytes are returned as base64. It also has synchronous OpenAI-compatible and WebUI-shaped endpoints. Prefer the asynchronous job pattern, serialize requests, add bounded polling/timeouts, and convert/store the base64 image. [API reference](https://github.com/leejet/stable-diffusion.cpp/blob/master/examples/server/api.md) · [Docker guide](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/docker.md).
3. **Use Docker networking correctly:** when n8n and the generator run in separate containers, `localhost` inside n8n means the n8n container. Put them on a shared Docker network and call the generator by service/container hostname, or use a published host port. Avoid exposing an unauthenticated model server on the public internet.
4. **For dependable video:** use n8n with FFmpeg to animate stills (pan/zoom, crossfades, captions, audio) and label the result as a slideshow/2D motion effect, **not generative T2V/I2V**. FFmpeg is a realistic local media-assembly tool, not a video model.
5. **For genuine video generation:** offload inference to a reachable GPU host or hosted provider, with the associated API credentials, cost/quota, network, data privacy, and checkpoint-license tradeoffs. n8n can submit a job, poll status, and retrieve the resulting file. AI Horde is an alternative for community T2I access, but has volunteer-capacity delays/limits and is not a dependable video backend.

**Bottom line:** on the screenshot's currently allocated Docker memory, even CPU text-to-image is marginal while the other containers remain active; a quantized SD 1.5 trial is the only well-supported local inference experiment here, not a promise of responsive use. True local T2V/I2V is not realistic on 8 GB host RAM with only about 3.5 GB shared among containers. If reliable interactive generation is a requirement, increase available memory and add supported GPU acceleration, or use remote inference.

## Corrections to the supplied pasted text

The original text should not be used unchanged. Important corrections:

1. **The claim that no end-to-end open T2V options exist is obsolete.** Wan2.1, CogVideoX and HunyuanVideo document runnable local T2V paths; Wan and other families also support I2V. “Open” still needs a code-vs-weights licensing caveat.
2. **First Order Motion Model is not a general image-to-video generator.** It animates a source image using a separate driving video/keypoint motion; it does not generate motion from a text prompt. Frame interpolation, avatar filters, and FFmpeg assembly are not interchangeable with learned generative I2V.
3. **The proposed old “Top 10” lists mix models, UIs, legacy code, APIs, interpolation software, and unfinished pipelines.** Stable Diffusion WebUI and ComfyUI are apps/runtimes, not models; DALL·E Mini, DALL·E Flow, Kandinsky-2, and assorted legacy experiments should not be presented as equal current choices without current-maintenance and exact-model verification.
4. **Do not assert generic minimum VRAM values.** Requirements vary by exact model, quantization, precision, resolution, frames, and offload settings. The ComfyUI code repository has no universal VRAM minimum. The Wan 8.19 GB statement is a specific 1.3B/480p case.
5. **Separate repository license from checkpoint license.** Examples: SVD code is MIT but weights have Stability AI terms; CogVideoX code is Apache-2.0 but weights have a separate license; HunyuanVideo has Tencent's community agreement; InvokeAI/ComfyUI code licenses do not determine each installed model's terms. “Open source” is not automatically synonymous with commercial-use permission.
6. **Correct the AI Horde description.** It is free subject to request limits, volunteer capacity, changing model availability, and queue time—not unlimited or guaranteed.
7. **Make the n8n network assumptions explicit.** HTTP Request is the straightforward integration mechanism, but a cloud n8n host cannot reach a workstation's `127.0.0.1`. Use same-network hosting or secured remote access. A `localhost` file path returned by a simple API is not downloadable by n8n; return a file/binary response or authenticated URL.
8. **Do not promise free GPU availability from notebook tiers.** Colab/Kaggle access, accelerators, quotas, and session duration change; availability is neither guaranteed nor an open-source feature.
9. **The pasted Diffusers snippet needs exact model access/version and explicit dtype handling.** Avoid implying `.to("cuda")` automatically selects FP16 for every pipeline, model, or GPU; use that model's current official loading guide and select supported precision deliberately.
10. **The old text2video slideshow is a custom media-editing pipeline, not generative video.** It can be useful for narration/slides, but label it accurately and do not rank it as an end-to-end T2V model.

## Sources checked

- ComfyUI: [GitHub repository](https://github.com/Comfy-Org/ComfyUI), [API examples](https://docs.comfy.org/development/comfyui-server/api-examples), [server routes](https://docs.comfy.org/development/comfyui-server/comms_routes), [Qwen-Image workflow](https://docs.comfy.org/tutorials/image/qwen/qwen-image), [Wan video workflow](https://docs.comfy.org/tutorials/video/wan/wan-video).
- InvokeAI: [repository](https://github.com/invoke-ai/InvokeAI), [workflow API](https://invoke.ai/development/guides/workflow-api/), [system requirements](https://invoke.ai/start-here/system-requirements/), [model terms](https://invoke.ai/concepts/models/), [experimental video](https://invoke.ai/features/video-generation/).
- Wan2.1: [repository](https://github.com/Wan-Video/Wan2.1), [T2V model card](https://huggingface.co/Wan-AI/Wan2.1-T2V-14B), [I2V model card](https://huggingface.co/Wan-AI/Wan2.1-I2V-14B-720P), [official ComfyUI example](https://comfyanonymous.github.io/ComfyUI_examples/wan/).
- CogVideoX: [repository](https://github.com/zai-org/CogVideo), [separate model license](https://github.com/zai-org/CogVideo/blob/main/MODEL_LICENSE), [T2V card](https://huggingface.co/zai-org/CogVideoX1.5-5B), [I2V card](https://huggingface.co/zai-org/CogVideoX1.5-5B-I2V).
- HunyuanVideo: [repository](https://github.com/Tencent-Hunyuan/HunyuanVideo), [license](https://raw.githubusercontent.com/Tencent-Hunyuan/HunyuanVideo/main/LICENSE.txt).
- LTX: [LTX-Video repository](https://github.com/Lightricks/LTX-Video), [LTX-Video model card and version-specific terms](https://huggingface.co/Lightricks/LTX-Video), [LTX-2.5 model card](https://huggingface.co/Lightricks/LTX-2.5).
- Stable Video Diffusion: [repository](https://github.com/Stability-AI/generative-models), [SVD-XT model card](https://huggingface.co/stabilityai/stable-video-diffusion-img2vid-xt), [SVD-XT terms](https://huggingface.co/stabilityai/stable-video-diffusion-img2vid-xt/blob/main/LICENSE.md).
- n8n and AI Horde: [n8n HTTP Request node](https://docs.n8n.io/integrations/builtin/core-nodes/n8n-nodes-base.httprequest), [AI Horde usage model](https://www.stablehorde.net/docs/image/learn/concepts_horde), [limits and model availability](https://www.stablehorde.net/docs/image/learn/concepts_migrating/).
- CPU-only/Docker fit: [stable-diffusion.cpp CPU build](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/build.md), [quantization/memory notes](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/quantization_and_gguf.md), [server API](https://github.com/leejet/stable-diffusion.cpp/blob/master/examples/server/api.md), [Docker guide](https://github.com/leejet/stable-diffusion.cpp/blob/master/docs/docker.md), [OpenVINO SD 1.5 INT8](https://huggingface.co/OpenVINO/stable-diffusion-v1-5-int8-ov), [OVMS image API](https://docs.openvino.ai/2026/model-server/ovms_demos_image_generation.html), [Tiny-SD CPU example](https://docs.openvino.ai/2023.3/notebooks/251-tiny-sd-image-generation-with-output.html), [Intel i5-1035G7 official graphics specification](https://www.intel.com/content/www/us/en/products/sku/196592/intel-core-i51035g7-processor-6m-cache-up-to-3-70-ghz/specifications.html), [CogVideoX-2B model](https://huggingface.co/zai-org/CogVideoX-2b), [CogVideoX Intel/CPU feasibility issue](https://github.com/zai-org/CogVideo/issues/222).
